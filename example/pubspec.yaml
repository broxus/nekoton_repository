name: example
description: Project for integration tests for nekoton_repository
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  nekoton_repository:
    path: ../

dev_dependencies:
  broxus_flutter_analysis:
    git:
      url: https://github.com/broxus/flutter_templates.git
      ref: main
  flutter_test:
    sdk: flutter
  http: ^0.13.6
  integration_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^5.0.0


flutter:
  uses-material-design: true
  generate: true
